[project]
name = "agent-reliability-orchestrator"
version = "0.1.0"
description = "Workflow state and task dispatch service"
requires-python = ">=3.12"
dependencies = [
  "agent-reliability-common",
  "agent-reliability-contracts",
  "fastapi>=0.116.0",
  "uvicorn[standard]>=0.35.0",
]

[tool.uv]
package = false

[tool.uv.sources]
agent-reliability-common = { workspace = true }
agent-reliability-contracts = { workspace = true }

[project]
name = "agent-reliability-api"
version = "0.1.0"
description = "HTTP API for jobs, status, and artifacts"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.116.0",
  "uvicorn[standard]>=0.35.0",
]

[tool.uv]
package = false

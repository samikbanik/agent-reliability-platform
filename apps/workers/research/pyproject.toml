[project]
name = "agent-reliability-worker-research"
version = "0.1.0"
description = "Worker that gathers and stores research artifacts"
requires-python = ">=3.12"
dependencies = [
  "agent-reliability-common",
  "agent-reliability-contracts",
]

[tool.uv]
package = false

[tool.uv.sources]
agent-reliability-common = { workspace = true }
agent-reliability-contracts = { workspace = true }

[project]
name = "agent-reliability-worker-planner"
version = "0.1.0"
description = "Worker that converts research goals into executable plans"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false

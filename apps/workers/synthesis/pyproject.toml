[project]
name = "agent-reliability-worker-synthesis"
version = "0.1.0"
description = "Worker that turns research artifacts into reports"
requires-python = ">=3.12"
dependencies = [
  "agent-reliability-common",
  "agent-reliability-contracts",
]

[tool.uv]
package = false

[tool.uv.sources]
agent-reliability-common = { workspace = true }
agent-reliability-contracts = { workspace = true }

[project]
name = "agent-reliability-worker-verifier"
version = "0.1.0"
description = "Worker that checks completeness, citations, and quality"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false
